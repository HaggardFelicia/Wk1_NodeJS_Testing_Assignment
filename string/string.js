//uppercase function to uppercase
const uppercase =(str)=>str.toUpperCase();
//lowercase function to lowercase
const lowercase =(str)=> str.toLowerCase();

module.exports={
    uppercase,
    lowercase
};